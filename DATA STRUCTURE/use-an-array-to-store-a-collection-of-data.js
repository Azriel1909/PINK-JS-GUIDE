/**
 * Array Data Structure
 * One-dimensional array (no other arrays nested within it).
 * Multidimensional Array (an array taht contains other arrays)
 */

let simpleArray = ['one', 9, 'three', false, true, undefined, null];
console.log(simpleArray.length);

let yourArray = ['Ximena', 39, 'Pinkhat', 'Arthur', 10, 'picnic', false];