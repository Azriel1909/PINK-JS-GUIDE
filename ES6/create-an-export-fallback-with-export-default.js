/**
 * Export Default
 * You will use this syntax if only one value is being
 * exported from a file. It is also used to create a fallback
 * value for a file or module.
 * 
 * Note: you cannot use export default with var, let, or const
 * 
 * Examples
 * 
 * // Named function
 * export default function add(x, y) {
 * return x + y;
 * }
 * 
 * // Anonymous function
 * export default function(x, y) {
 * return x + y;
 * }
 * 
 */
export default function subtract(x, y) {
  return x - y;
}