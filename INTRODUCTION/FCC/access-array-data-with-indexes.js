// We can access the data inside arrays using indexes, arrays use Zero-based indexing like strings.

const array = [50, 60, 70];
console.log(array[0]);
const data = array[1];
console.log(data);

const myArray = [50, 60, 70];
console.log(myArray[0]);
const myData = myArray[0];
console.log(myData);



