// In javaScript, we use the '+' Concatenation Operator to concatenate Strings.
const ourStr = "I come first. " + "I come second.";
console.log(ourStr);

const myStr = "This is the start." + ' This is the end.';
console.log(myStr);
