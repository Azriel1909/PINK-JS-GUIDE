/** We can use the += operator to concatenate a string onto the end of an existing string variable. This can be helpful to break a long string over several lines */

let ourStr = "I come first. ";
ourStr += "I come second.";
console.log(ourStr);

let myStr = 'This is the first sentence.';
myStr += ' This is the second sentence.'
console.log(myStr);
