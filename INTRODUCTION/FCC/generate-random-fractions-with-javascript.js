/**
 * Random Fractions
 * We use Math.random() function that generates a random
 * decimal number between 0(inclusive) and 1(exclusive). 
 * It can return a 0 but never return a 1
 */

function randomFraction(n) {
  return Math.random(n);
}