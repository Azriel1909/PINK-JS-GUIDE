// For loops
const myArray = [];
/**
for (a; b; c), where a is the initialization statement, b is the
condition statement, and c is the final expression.
 */
for (let i = 1; i < 6; i++) {
  myArray.push(i);
}

console.log(myArray);