// The entries of arrays are mutable and can be changed freely (even if the arrays was declared with const).

const ourArray = [50, 40, 30];
ourArray[0] = 15;
console.log(ourArray);

const myArray = [18, 64, 99];
myArray[0] = 45;
console.log(myArray);
