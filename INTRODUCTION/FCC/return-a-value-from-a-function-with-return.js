// We can use return to send a value back out of a funtion.

function plusThree(num) {
  return num + 3;
}

const answer = plusThree(5);
console.log(answer);

function timesFive(n) {
  return n * 5;
}

const answerTimesFive = timesFive(5);
console.log(answerTimesFive);