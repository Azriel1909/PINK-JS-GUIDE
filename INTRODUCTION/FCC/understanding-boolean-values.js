// Boolean Value (another data type), booleand may only be True or False
// They're basically little on-off switches 
// These Two states are mutually exclusive

function welcomeToBooleans() {
  return true;
}