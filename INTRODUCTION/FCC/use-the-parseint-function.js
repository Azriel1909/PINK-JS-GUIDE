/**
 * ParseInt Function parses a string and returns an integer
 * const a = parseInt("007");
 * 
 * It converts the string '007' to the integer 7, if the string cannot be converted into a number, then it returns a NaN
 */

function convertToInteger(str) {
  return parseInt(str);
}

convertToInteger("56");